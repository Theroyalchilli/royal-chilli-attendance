# Same Supabase project as royal-chilli-pos (the two apps share one database).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# MUST match royal-chilli-pos's JWT_SECRET — so a manager session issued by
# either app validates in the other once both are on *.royalchilli.com.
JWT_SECRET=
